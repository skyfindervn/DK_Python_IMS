[project]
name = "shared"
version = "0.1.0"
description = "Shared internal library for monorepo"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared"]
